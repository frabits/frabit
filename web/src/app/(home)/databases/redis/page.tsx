import Redis from '@/components/pages/databases/redis';

const Page = () => {
    return (
        <>
            <h1 className="sr-only">Support database</h1>
            <Redis />
        </>
    );
};

export default Page;
