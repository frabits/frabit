import Postgresql from '@/components/pages/databases/postgresql';

const Page = () => {
    return (
        <>
            <h1 className="sr-only">Support database</h1>
            <Postgresql />
        </>
    );
};

export default Page;
